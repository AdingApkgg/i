[package]
name = "i-device"
version = "0.0.0"
edition.workspace = true
license.workspace = true

[lib]
name = "device"
path = "src/lib.rs"

[dependencies]
i-core = { workspace = true }
axum = { workspace = true }
serde_json = { workspace = true }
